global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/aria-alerts.yml

scrape_configs:
  - job_name: aria
    metrics_path: /health/metrics/
    authorization:
      credentials_file: /tmp/aria-metrics-token
    static_configs:
      - targets: ["api:8000"]
